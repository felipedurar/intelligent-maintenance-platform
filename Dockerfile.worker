FROM python:3.12-slim

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1
ENV PYTHONPATH=/app/src

WORKDIR /app

RUN apt-get update \
    && apt-get install -y --no-install-recommends curl \
    && rm -rf /var/lib/apt/lists/*

COPY pyproject.toml README.md prefect.yaml ./
COPY docs ./docs
COPY docs_governance ./docs_governance
COPY evaluation ./evaluation
COPY src ./src
COPY scripts ./scripts

RUN pip install --no-cache-dir --upgrade pip \
    && pip install --no-cache-dir "torch>=2.4.0,<3.0.0" --index-url https://download.pytorch.org/whl/cpu \
    && pip install --no-cache-dir .

CMD ["prefect", "worker", "start", "--pool", "datathon-local"]
