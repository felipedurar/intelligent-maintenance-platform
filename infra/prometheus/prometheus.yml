global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: platform-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - platform-api.im.local:8000
